<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Using a stable MaterialComponents base to ensure successful linking -->
    <style name="Theme.SafeSync" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Optional: Match Material 3 colors if needed -->
        <item name="colorPrimary">@android:color/holo_purple</item>
    </style>
</resources>
